# Chain Pet

A digital pet that lives on Stellar testnet. Its life history is on-chain and cannot be reset.

```
pnpm install
pnpm dev
```

Optional: `VITE_TIME_SCALE=60` in `.env.local` makes one real minute count as one hour for the demo.
